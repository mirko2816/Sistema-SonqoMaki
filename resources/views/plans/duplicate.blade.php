<x-layouts.authenticated title="Duplicar plan"><x-page-header title="Duplicar {{ $plan->name }}" description="Se copiarán rutinas, ejercicios y posiciones; no se copiará el enlace ni el estado."/><form method="POST" action="{{ route('plans.duplicate',$plan) }}" class="mt-8 max-w-2xl space-y-6">@csrf<x-card class="space-y-5 p-5 sm:p-6"><div><label for="patient_id" class="block text-sm font-semibold">Paciente de destino</label><select id="patient_id" name="patient_id" required class="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3">@foreach($patients as $patient)<option value="{{ $patient->id }}" @selected(old('patient_id',$plan->patient_id)==$patient->id)>{{ $patient->full_name }}</option>@endforeach</select>@error('patient_id')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror</div><div><label for="name" class="block text-sm font-semibold">Nombre de la copia</label><input id="name" name="name" required maxlength="160" value="{{ old('name','Copia de '.$plan->name) }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3"></div><p class="text-sm text-slate-600">Las fechas se copian sin desplazamiento, como indica el caso de uso. La copia quedará en pausa para que las revises.</p></x-card><div class="flex gap-3 justify-end"><a href="{{ route('plans.show',$plan) }}" class="inline-flex min-h-11 items-center px-5">Cancelar</a><button class="rounded-xl bg-brand-600 px-5 text-sm font-semibold text-white">Crear copia independiente</button></div></form></x-layouts.authenticated>
