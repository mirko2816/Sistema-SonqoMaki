<x-layouts.authenticated title="Copiar plantilla"><x-page-header title="Copiar plantilla" :description="'Crea una rutina independiente dentro de '.$plan->name"/><form method="POST" action="{{ route('plans.routines.copy',$plan) }}" class="mt-8 max-w-3xl space-y-6">@csrf<x-card class="grid gap-5 p-5 sm:grid-cols-2 sm:p-6"><div class="sm:col-span-2"><label for="routine_template_id" class="block text-sm font-semibold">Plantilla activa</label><select id="routine_template_id" name="routine_template_id" required class="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3"><option value="">Selecciona</option>@foreach($templates as $template)<option value="{{ $template->id }}" @selected(old('routine_template_id')==$template->id)>{{ $template->name }} ({{ $template->exercises_count }} ejercicios)</option>@endforeach</select>@error('routine_template_id')<p class="mt-2 text-sm text-red-700">{{ $message }}</p>@enderror</div><div><label for="starts_on" class="block text-sm font-semibold">Inicio</label><input id="starts_on" name="starts_on" type="date" min="{{ $plan->starts_on->format('Y-m-d') }}" max="{{ $plan->ends_on->format('Y-m-d') }}" required value="{{ old('starts_on',$plan->starts_on->format('Y-m-d')) }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3"></div><div><label for="ends_on" class="block text-sm font-semibold">Fin</label><input id="ends_on" name="ends_on" type="date" min="{{ $plan->starts_on->format('Y-m-d') }}" max="{{ $plan->ends_on->format('Y-m-d') }}" required value="{{ old('ends_on',$plan->ends_on->format('Y-m-d')) }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3"></div>@error('starts_on')<p class="sm:col-span-2 text-sm text-red-700">{{ $message }}</p>@enderror<p class="sm:col-span-2 text-sm text-slate-600">El nombre, ejercicios, configuración y posiciones se copiarán. Los cambios posteriores no afectarán la plantilla.</p></x-card><div class="flex justify-end gap-3"><a href="{{ route('plans.show',$plan) }}" class="inline-flex min-h-11 items-center px-5">Cancelar</a><button class="rounded-xl bg-brand-600 px-5 text-sm font-semibold text-white">Copiar plantilla</button></div></form></x-layouts.authenticated>
