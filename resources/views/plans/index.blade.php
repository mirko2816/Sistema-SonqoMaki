<x-layouts.authenticated title="Planes"><x-page-header title="Planes" description="Consulta y administra los planes independientes de cada paciente."><x-slot:actions><a href="{{ route('plans.create') }}" class="inline-flex min-h-11 items-center justify-center rounded-xl bg-brand-600 px-5 text-sm font-semibold text-white">Nuevo plan</a></x-slot:actions></x-page-header>
<form class="mt-8 flex gap-2" method="GET"><label for="search" class="sr-only">Buscar planes</label><input id="search" name="search" value="{{ $search }}" placeholder="Buscar por plan o paciente" class="min-h-11 min-w-0 flex-1 rounded-xl border border-slate-300 px-3"><button class="rounded-xl bg-slate-900 px-5 text-sm font-semibold text-white">Buscar</button></form>
<div class="mt-6 space-y-3">@forelse($plans as $plan)<a href="{{ route('plans.show',$plan) }}" class="block rounded-2xl border border-slate-200 bg-white p-5 shadow-sm hover:border-brand-300"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="font-semibold">{{ $plan->name }}</h2><p class="mt-1 text-sm text-slate-600">{{ $plan->patient->full_name }} · {{ $plan->starts_on->format('d/m/Y') }} – {{ $plan->ends_on->format('d/m/Y') }} · {{ $plan->routines_count }} rutina(s)</p></div><span class="w-fit rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold">{{ ['active'=>'Activo','paused'=>'En pausa','finished'=>'Finalizado'][$plan->status] }}</span></div></a>@empty<x-card><x-empty-state title="No hay planes" description="Crea el primer plan para un paciente activo."/></x-card>@endforelse</div><div class="mt-6">{{ $plans->links() }}</div></x-layouts.authenticated>
